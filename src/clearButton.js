const btn = document.getElementById("clrBtn");

btn.onclick = () => {
    
    firstArrow.reset(context);
    firstCircle.reset(context);

    secondArrow.reset(context);
    secondCircle.reset(context);

    thirdArrow.reset(context);
    thirdCircle.reset(context);

    fourthArrow.reset(context);
    fourthCircle.reset(context);
}